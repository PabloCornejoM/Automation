name: automation_venv
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - pillow
  - opencv
  - pip
  - pip:
      - pyautogui
      - keyboard
      - mouse
      - pygetwindow
      - pymsgbox
      - ipykernel
      - crontab

# To create the environment, run:
# conda env create -f environment.yml